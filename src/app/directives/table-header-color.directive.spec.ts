import { TableHeaderColorDirective } from './table-header-color.directive';

describe('TableHeaderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHeaderColorDirective();
    expect(directive).toBeTruthy();
  });
});
